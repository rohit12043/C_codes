// We can also define constansts using the "#define" directive

#include <stdio.h>
#define val 10 // job of preprocessor is to replace the constant with the actual value.

int	main()
{
printf("%d \n", val);
}

